# MindBridge AI - Backend

Backend for MindBridge AI. Configure `.env` from `.env.example`, then run:

```bash
cd backend
npm install
npm run dev
```

Expose: REST API + Socket.IO signaling. Ensure `MONGO_URI` and `OPENAI_API_KEY` are set.
